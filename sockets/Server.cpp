//Server.cpp
#include "Server.h"

Server::Server(){
	//server = TCPServer(the correct stuff);
}

Server::Daemon(){
	//listen for input
	  //check if Socket in map
	    //if not add new (ID,Socket) to map
	  //access Socket from map
	  //do thing and send string back if needed
}

